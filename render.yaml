services:
  - type: web
    name: overunder
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: overunder-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
